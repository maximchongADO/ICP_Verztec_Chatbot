# Email Configuration for HR Escalation
# Copy this file to .env and fill in your actual credentials
# NEVER commit the .env file to version control!

# Gmail SMTP Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SENDER_EMAIL=your_email@gmail.com
SENDER_APP_PASSWORD=your_16_character_app_password

# HR Notification Settings
HR_EMAIL=hr_representative@company.com

# Alternative Email Services (uncomment and configure as needed)
# For Outlook/Hotmail:
# SMTP_SERVER=smtp-mail.outlook.com
# SMTP_PORT=587
# SENDER_EMAIL=your_email@outlook.com
# SENDER_APP_PASSWORD=your_password

# For Yahoo Mail:
# SMTP_SERVER=smtp.mail.yahoo.com
# SMTP_PORT=587
# SENDER_EMAIL=your_email@yahoo.com
# SENDER_APP_PASSWORD=your_app_password

# Security Setup Instructions:
# 1. For Gmail: Enable 2FA and generate an App Password
#    - Go to: Google Account → Security → 2-Step Verification → App passwords
#    - Generate a 16-character app password and use it above
# 2. For other providers: Check their documentation for app-specific passwords
# 3. Keep your .env file secure and never share it
# 4. Make sure .env is in your .gitignore file
